CREATE TABLE IF NOT EXISTS FACT_SALES (
    SALE_ID INT,
    STORE_ID INT,
    PRODUCT_ID INT,
    DATE_KEY DATE,
    REVENUE DOUBLE,
    QUANTITY INT,
    COST DOUBLE,
    DISCOUNT_PCT DOUBLE
) USING DELTA;
